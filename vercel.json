{
  "buildCommand": "cd frontend && npm install && npm run build && rm -rf ../public && mkdir -p ../public && cp -R dist/. ../public/ && rm -rf ../api/static && mkdir -p ../api/static && cp -R dist/. ../api/static/",
  "rewrites": [
    { "source": "/health", "destination": "/api/index" },
    { "source": "/api/v1/(.*)", "destination": "/api/index" }
  ]
}
