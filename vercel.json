{
  "buildCommand": "cd frontend && npm install && npm run build && rm -rf ../api/static && mkdir -p ../api/static && cp -R dist/. ../api/static/",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
